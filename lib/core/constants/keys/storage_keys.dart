abstract final class StorageKeys {
  static const accessToken = 'session.access_token';
  static const refreshToken = 'session.refresh_token';
  static const userId = 'session.user_id';
  static const userEmail = 'session.user_email';
  static const sessionExpiresAt = 'session.expires_at';

  static const moviesLastFetchAt = 'movies.last_fetch_at';
}
